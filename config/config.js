const config = {
    recordAudio: true, // Record audio from microphone

    displaySpeed: true, // Display speed in real-time

    recordingDuration: 10000, // Add appropriate duration in milliseconds

    enableFrontCamera: false, // Enable front camera recording

    enableRearCamera: true, // Enable rear camera recordings

    timeToLive: 30, // Time to live for footage in days
};

export default config;
